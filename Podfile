# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Budgetly' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Budgetly
  pod "Parse"
  pod "AlamofireImage"
  pod "Charts"
  pod "RSSelectionMenu"
  
  target 'BudgetlyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BudgetlyUITests' do
    # Pods for testing
  end

end
