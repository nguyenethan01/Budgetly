import * as React from 'react';
declare const Wrapper: ({ children }: {
    children: any;
}) => JSX.Element;
interface Props {
    setRef?: (ref: any) => void;
}
export declare const Container: React.SFC<Props>;
export default Wrapper;
